node_modules/
dist/
.turbo/
.tmp/
**/.next/
**/next-env.d.ts
*.tsbuildinfo
.env
.env.local
.env.*.local
bin/aas
.DS_Store
.vercel
.env*

# secrets / build artifacts (never commit)
g-oauth.txt
bin/

# credential files (never commit)
*.pem
.waffo*
waffo-*.pem
*-private.*

# Local test artifacts (installed skills/mcp/providers)
.agents/

# OpenNext Cloudflare build output
.open-next/
apps/store/.open-next/

# wrangler local state
.wrangler/
apps/*/.wrangler/

# Workers local secrets (keep .dev.vars.example tracked)
.dev.vars
apps/*/.dev.vars

# local keys (never commit)
keys/

# e2e / test secrets: provider API keys, oauth tokens, private keys (never commit)
test/provider/
test/*.txt
test/*.pem

# Apple / provider secrets (never commit)
.secrets/
